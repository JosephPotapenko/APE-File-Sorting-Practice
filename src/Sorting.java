/*
 * Write the sortArray method below. No calls to sort methods
 * provided by the API are allowed.  You must write your own
 * sort algorithm.
 * 
 * It is passed an array of Comparable, so you can use the behaviors
 * that are guaranteed by the Comparable interface as part of your
 * algorithm.
 * 
 * See the unit test in SortingTests or the tests in SortingMain if
 * you do not understand unit tests for how your sort routine should behave.
 * 
 */

public class Sorting {
	public static void sortArray(Comparable [] array){

		for(int i = 0; i< array.length; i++)
		{
			for(int j = 0; i < array.length; i++)
			{
				if(array[j + 1] < array[j])
				{
					int temp
				}
			}
		}
	}

}
